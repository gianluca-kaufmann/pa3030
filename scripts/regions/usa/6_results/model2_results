#!/usr/bin/env python3
"""Thin wrapper for USA Model 2 results reporting."""

import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[4]
if str(REPO_ROOT) not in sys.path:
    sys.path.insert(0, str(REPO_ROOT))

from scripts.regions.shared.results import run_region_results


if __name__ == "__main__":
    run_region_results("usa")
