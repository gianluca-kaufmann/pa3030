# Regional pipelines (South America, USA, etc.)

Pipeline outputs (results, metrics, scored parquets) go under the **repository root** `outputs/` directory, not under `scripts/regions/`. Do not create or use `scripts/regions/outputs/`.
