#!/usr/bin/env python3
"""Cross-Continental Transfer — SA ↔ USA (Layer 3)

Trains on South America, evaluates on USA (SA→USA); then trains on USA and
evaluates on South America (USA→SA).  Only features common to both regions
are used.  Both directions run sequentially in a single job.

Usage:
    python scripts/regions/south_america/6_evaluation/spatial_CV_3

SLURM:
    sbatch slurm/south_america/spatial_cv_transfer.slurm

Output:
    outputs/south_america/results/spatial_cv/
        transfer_results_<timestamp>.json
        transfer_results_<timestamp>.txt
        transfer_south_america_to_usa_<timestamp>.parquet
        transfer_usa_to_south_america_<timestamp>.parquet

See scripts/regions/shared/evaluation/spatial_cv_core.py for full implementation.
"""

from scripts.regions.shared.evaluation.spatial_cv_core import run_transfer_main

if __name__ == "__main__":
    run_transfer_main()
