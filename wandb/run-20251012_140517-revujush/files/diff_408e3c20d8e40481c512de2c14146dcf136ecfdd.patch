diff --git a/scripts/merging/merge_2012_optimized b/scripts/merging/merge_2012_optimized
index 71bff7e..f0b02d4 100755
--- a/scripts/merging/merge_2012_optimized
+++ b/scripts/merging/merge_2012_optimized
@@ -1,4 +1,4 @@
-#!/usr/bin/env python3
+#!/usr/bin/python3
 """
 Optimized merge script for South America 1×1 km datasets into a yearly tile-year panel (2012–2024).
 
@@ -27,6 +27,9 @@ import pandas as pd
 import wandb
 
 
+# Set W&B API key
+os.environ["WANDB_API_KEY"] = "3e5c388d45c1632fa75a63cb0840fbccb1470412"
+
 DATA_ROOT = Path("/Users/gianluca/Desktop/Master's Thesis/code/data")
 READY_ROOT = DATA_ROOT / "ready"
 OUTPUT_ROOT = Path("/Users/gianluca/Desktop/Master's Thesis/code/outputs/Results")
@@ -351,19 +354,22 @@ def process_dataset(dataset: str, entries: Dict[Optional[int], List[Path]], ref_
 
 def main():
     """Main processing function with progress monitoring."""
-    total_start_time = time.time()
-    log_progress("Starting optimized merge process...")
-    
-    # Initialize wandb
+    # Initialize wandb FIRST before any processing
+    print("🔄 Initializing Weights & Biases connection...")
     wandb.init(
         project="merging",
-        entity=os.environ.get("WANDB_ENTITY"),
+        entity="gikaufmann-eth-z-rich",
+        save_code=True,
         config={
             "years": YEARS,
             "data_root": str(DATA_ROOT),
             "output_root": str(OUTPUT_ROOT),
         }
     )
+    print("✅ Weights & Biases connected successfully!")
+    
+    total_start_time = time.time()
+    log_progress("Starting optimized merge process...")
     
     # Setup reference
     ref_path, ref_src = open_reference_wdpa()
